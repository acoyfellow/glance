{
  "name": "glance",
  "private": true,
  "type": "module",
  "scripts": {
    "server": "bun --watch run server.ts",
    "observe": "bun run src/git-observer.ts",
    "demo:agents": "bun run demo/agent-presence.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  },
  "devDependencies": {}
}
